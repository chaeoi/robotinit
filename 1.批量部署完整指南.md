# Jetson AGX Orin 无头系统制作与批量部署完整指南

> 环境假设：**JetPack 6.x (L4T 36.x) + 外置 NVMe SSD**
> 目标：制作一台"最纯净的无头黄金母机"，再批量克隆部署到多台设备。
> 全流程使用官方 `Linux_for_Tegra` (BSP) 工具，不依赖第三方软件，全程无需显示器/键鼠。

---

## 目录

- [一、总体路线图](#一总体路线图)
- [二、开始前必须确认的信息](#二开始前必须确认的信息)
- [三、无头系统的三种官方配置方式](#三无头系统的三种官方配置方式)
- [四、制作"最纯净无头"黄金母机](#四制作最纯净无头黄金母机)
- [五、批量部署（黄金母机克隆）](#五批量部署黄金母机克隆)
- [六、仓库脚本](#六仓库脚本)
- [七、命令速查表](#七命令速查表)
- [八、常见坑与注意事项](#八常见坑与注意事项)

---

## 一、总体路线图

```
制作阶段（黄金母机）
┌─ ① 生成最小 headless rootfs（nv_build_samplefs --flavor minimal）
├─ ② 解包进 rootfs/ + apply_binaries.sh
├─ ③ 锁定 multi-user.target（永久无头）
├─ ④ l4t_create_default_user.sh 预置用户（跳过 oem-config 向导）
├─ ⑤ initrd flash 刷进母机 NVMe
└─ ⑥ 在纯净系统上只装需要的组件（CUDA/TensorRT/应用）

批量部署阶段（克隆）
┌─ ⑦ 运行 clean.sh，清理母机状态并自动关机
├─ ⑧ 母机进 recovery → backup_restore 读出黄金镜像
├─ ⑨ 目标机进 recovery → backup_restore 恢复黄金镜像或 massflash
├─ ⑩ 目标机首次开机 → 通过 SSH 执行 init.sh m02
└─ ⑪ init.sh 设置 hostname、重建 machine-id/SSH key、清状态并重启
```

**核心理念**：

- **纯净 = 从最小系统往上装**，而不是刷完整桌面版再删 GUI（永远删不干净）。
- **无头 = 刷机前预置用户 + 锁定 multi-user**，开机不弹配置向导。
- **批量 = 一台母机克隆 + 目标机首启后执行一个脚本完成所有差异化身份初始化**。

---

## 二、开始前必须确认的信息

在母机上执行，确认两个关键参数（决定命令里的精确取值）：

```bash
# 1) 确认精确 L4T 版本（36.3？36.4？minor 版本必须和主机 BSP 完全一致）
cat /etc/nv_tegra_release
# 输出示例：R36 (release), REVISION: 3.0 ...

# 2) 确认 rootfs 到底在哪个 NVMe 设备/分区
lsblk -o NAME,SIZE,MOUNTPOINT | grep -E 'nvme|/'
findmnt /        # 看 / 挂在 nvme0n1p1 还是别的
```

> ⚠️ **关键前提**：主机上的 `Linux_for_Tegra` BSP 版本必须与母机 L4T 版本**完全一致**（大版本 + 小版本）。版本不匹配是克隆失败/变砖最常见的原因。

---

## 三、无头系统的三种官方配置方式

首次开机时，`oem-config`（首次配置向导）需要在某处运行。官方支持三种无头方式：

| 方法                  | 需要接线          | 需要交互         | 适用场景              |
| --------------------- | ----------------- | ---------------- | --------------------- |
| ① 串口 oem-config     | USB-C（虚拟串口） | 要（串口里配置） | 单台/少量，手动配     |
| ② USB Device Mode SSH | USB-C             | 看情况           | 配合③，无头远程管理   |
| ③ 预置用户跳过向导    | 无（刷机时写入）  | **完全不需要**   | **批量部署、量产** ⭐ |

### 方法一：串口控制台（Serial Console）

AGX Orin devkit 的 **USB-C 刷机口自带 USB 转串口**，无需额外 USB-TTL 线。

```bash
# 主机 USB-C 连 Orin 刷机口，正常上电（不进 recovery）
ls /dev/ttyACM*                       # 通常是 /dev/ttyACM0
sudo screen /dev/ttyACM0 115200       # 波特率 115200
# 或：sudo minicom -D /dev/ttyACM0 -b 115200
# 退出 screen：Ctrl+A 然后按 K
```

### 方法二：USB Device Mode（虚拟网卡 SSH）

Orin 通过 USB-C 连主机时虚拟出一块网卡，Jetson 固定 IP 为 **192.168.55.1**。

```bash
ssh <用户名>@192.168.55.1
```

> 需要系统里已有用户（配合方法三或方法一走完向导后使用）。

### 方法三：刷机前预置用户，跳过向导 ⭐（量产首选）

真正的"零交互"，刷出来的机器开机直接进系统。详见下方 [阶段 ④](#阶段--预置用户开机免交互)。

---

## 四、制作"最纯净无头"黄金母机

### 阶段 0：主机（x86 Ubuntu）准备

需要一台 **x86_64 Ubuntu 20.04/22.04** 主机（不能用 Jetson 自己刷自己）。

```bash
# 安装依赖
sudo apt update
sudo apt install -y libxml2-utils python3 sshpass abootimg nfs-kernel-server \
                    binfmt-support qemu-user-static device-tree-compiler

# 下载与母机同版本的 BSP（从 https://developer.nvidia.com/embedded/jetson-linux）
#   - Jetson_Linux_R36.x.x_aarch64.tbz2        (Driver Package / BSP)
#   - Tegra_Linux_Sample-Root-Filesystem_R36.x.x_aarch64.tbz2  (desktop 版，本方案不用它)

tar xf Jetson_Linux_R36.x.x_aarch64.tbz2       # 得到 Linux_for_Tegra/
cd Linux_for_Tegra
```

### 阶段 ①：生成最小 headless rootfs

> **为什么不用下载的 sample rootfs？** 官网那个是 **desktop 版**，自带 GNOME/gdm3/大量 GUI 包，做母机不纯净。要自己生成 minimal 版。

```bash
cd Linux_for_Tegra

sudo ./tools/samplefs/nv_build_samplefs.sh \
     --abi aarch64 \
     --distro ubuntu \
     --version jammy \
     --flavor minimal
```

- `--flavor minimal` → **最小系统**，纯 CLI，无桌面/显示管理器
- `--flavor basic` → 稍全，但仍无桌面
- `--version jammy` → Ubuntu 22.04（JetPack 6 对应）

> ⚠️ 先跑 `sudo ./tools/samplefs/nv_build_samplefs.sh -h` 确认你这版支持的 flavor 列表。
> 脚本会在 `tools/samplefs/` 下生成 `sample_fs.tbz2`。

### 阶段 ②：把最小 rootfs 装进刷机目录

```bash
cd Linux_for_Tegra

sudo rm -rf rootfs/*
sudo tar xpf tools/samplefs/sample_fs.tbz2 -C rootfs/
sudo ./apply_binaries.sh      # 应用 NVIDIA BSP（内核模块、bootloader、库）
```

### 阶段 ③：锁定无头（默认 multi-user 目标）

```bash
sudo ln -sf /lib/systemd/system/multi-user.target \
            rootfs/etc/systemd/system/default.target
```

> 即使以后误装 GUI 包，机器也不会自动进图形界面，永远停在 tty/SSH。

### 阶段 ④：预置用户（开机免交互）

无头系统绝不能弹 oem-config（没屏幕会卡死）。用官方脚本预置用户：

```bash
cd Linux_for_Tegra

sudo tools/l4t_create_default_user.sh \
     -u ubuntu \
     -p ubuntu \
     -n jetson-master \
     -a \
     --accept-license
# 全部参数：sudo tools/l4t_create_default_user.sh -h
```

上例用户名和密码都是 `ubuntu`，`jetson-master` 是母机临时 hostname。`-n` 可以给当前 rootfs 写入 hostname，但一个 rootfs 或镜像只能保存一个名称。克隆同一黄金镜像时，目标机的唯一 hostname 在阶段⑩按产线命名逻辑传给 `init.sh`。

### 阶段 ⑤：刷入母机 NVMe

母机进 recovery（USB-C 刷机口 + 按住 **Force Recovery**（中间按钮）上电，2 秒后松开），主机确认：

```bash
lsusb | grep -i nvidia        # 应看到 ID 0955:7023 NVIDIA Corp. APX
```

刷机：

```bash
cd Linux_for_Tegra

sudo ./tools/kernel_flash/l4t_initrd_flash.sh \
     --external-device nvme0n1p1 \
     -c ./tools/kernel_flash/flash_l4t_t234_nvme.xml \
     --showlogs \
     --network usb0 \
     jetson-agx-orin-devkit external
```

刷完开机 → `ssh ubuntu@192.168.55.1`（USB 虚拟网卡）→ 得到干净无头母机。

### 阶段 ⑥：只装需要的组件

从"零 GUI"往上搭，装什么由你决定：

```bash
sudo apt update

# 只装运行时（不拉桌面）
sudo apt install -y nvidia-jetpack-runtime      # CUDA/cuDNN/TensorRT 运行时
# 或整套（仍无桌面，因 rootfs 无桌面依赖）：
# sudo apt install -y nvidia-jetpack

# 装应用依赖、Docker、推理框架等……
```

> `nvidia-jetpack` meta 包**不会**装回桌面。桌面走的是 `ubuntu-desktop` / `nvidia-l4t-graphics` 那条线，minimal rootfs 里没有，不主动装就永远没有。

---

## 五、批量部署（黄金母机克隆）

> ⚠️ **NVMe + JetPack 6.x 必须走 `backup_restore` / `initrd flash` 工具链**，不能用老的 `flash.sh -r -k APP`（那是 eMMC 的老流程）。

### 阶段 ⑦：母机内部净化

直接克隆会复制母机的 SSH host key、machine-id、随机种子、NetworkManager 主机密钥、Jetson USB Device Mode MAC、蓝牙状态、日志和运行记录。**必须先清理**。

为了保证目标机首次开机后还能通过密码 SSH 登录，母机净化阶段**暂时保留 SSH host key 和 hostname**。目标机开机后运行 `init.sh`，一次性设置目标 hostname，并重建 machine-id 与 SSH host key。

仓库内的 `clean.sh` 执行以下操作：

1. 确认当前系统是 Jetson Linux。
2. 检查 netplan 和 NetworkManager profile；发现固定 MAC、固定接口名或静态 IP 时拒绝继续。
3. 清空 machine-id，并保留 SSH host key 供目标机首次 SSH 登录。
4. 清除 random-seed、systemd credential secret、libuuid 状态和 Jetson USB Device Mode MAC。
5. 清除 NetworkManager、DHCP、蓝牙、rfkill、timesync、timer、chrony 和 cloud-init 运行状态。
6. 清除 journal、rsyslog、crash、coredump、Docker 日志、APT 缓存、用户缓存和历史。
7. 停止 NetworkManager 和日志服务，进行最终清理、同步磁盘并自动关机。

推荐从刷机主机把脚本复制到母机 `/tmp`。脚本会清空 `/tmp`，因此不会留在黄金镜像：

```bash
scp clean.sh ubuntu@192.168.10.142:/tmp/
ssh -t ubuntu@192.168.10.142 'sudo bash /tmp/clean.sh --check'
ssh -t ubuntu@192.168.10.142 'sudo bash /tmp/clean.sh'
```

脚本要求输入 `CLEAN` 确认，然后自动关机。自动化流程可以使用：

```bash
sudo bash /tmp/clean.sh --yes
```

需要同时删除 APT 软件包索引、进一步缩小镜像时使用：

```bash
sudo bash /tmp/clean.sh --purge-apt-lists
```

> **顺序很重要**：完成软件安装和配置 → 最后运行 `clean.sh` → 脚本自动关机 → 立即读取镜像。母机如果再次正常开机，machine-id、random-seed、USB MAC 和 NetworkManager 状态会重新生成，必须重新净化。

> 日志和运行状态可能在关机过程中产生少量新记录。要求完全空白时，可在阶段⑧备份完成后，将镜像挂载到主机上再做一次离线清理。

### 阶段 ⑧：从母机 NVMe 制作黄金镜像

母机进 recovery，主机执行：

```bash
cd Linux_for_Tegra

sudo systemctl stop udisks2.service
sudo service nfs-kernel-server start

sudo ./tools/backup_restore/l4t_backup_restore.sh \
     --network usb0 \
     -e nvme0n1 \
     -b -c \
     jetson-agx-orin-devkit
```

- `-b` = backup（读）
- `-c` = 同时转换成可供 massflash 使用的 initrd flash 镜像
- `-e nvme0n1` = 外置 NVMe 设备（用阶段二 `lsblk` 确认的设备名）
- 镜像输出到 `tools/backup_restore/images/`

> `-c` 支持单个外置存储设备，本方案只备份 `nvme0n1`，符合要求。只做单台恢复、不制作 massflash 包时可以去掉 `-c`。
>
> 先查看 `tools/backup_restore/README_backup_restore.txt`，并运行 `sudo ./tools/backup_restore/l4t_backup_restore.sh -h` 核对当前 36.x 小版本。
> **读出的镜像先备份一份到安全位置，这就是"母版"。**

### 阶段 ⑨：刷入目标机

目标机进入 recovery。单台恢复直接使用 NVIDIA 原版 `backup_restore`，它会把阶段⑧的黄金镜像写回 NVMe：

```bash
cd /home/ubuntu/Linux_for_Tegra

sudo ./tools/backup_restore/l4t_backup_restore.sh \
     --network usb0 \
     -e nvme0n1 \
     -r \
     jetson-agx-orin-devkit
```

不要用普通的 `l4t_initrd_flash.sh ... external` 代替上面的恢复命令；普通刷机命令默认使用 BSP 当前生成的 rootfs，并不等于恢复阶段⑧的黄金备份。

如需并发量产，阶段⑧使用 `-b -c` 后，先生成基于备份镜像的 massflash 包：

```bash
sudo ./tools/kernel_flash/l4t_initrd_flash.sh \
     --use-backup-image \
     --no-flash \
     --massflash 5 \
     --network usb0 \
     jetson-agx-orin-devkit internal
```

然后把目标机置于 recovery 并执行：

```bash
sudo ./tools/kernel_flash/l4t_initrd_flash.sh \
     --flash-only \
     --massflash 5 \
     --network usb0
```

这里的 `internal` 是 R36.5 `README_backup_restore.txt` 给出的转换备份 massflash 用法；实际恢复哪些分区由 `--use-backup-image` 生成的备份包决定。`5` 是最大并发数量，按工位实际数量调整。

### 阶段 ⑩：目标机开机后执行 init.sh

目标机第一次开机后，先通过克隆来的 SSH host key 和预置用户密码登录。再由刷机主机按你们的命名逻辑生成 hostname，例如 `m02`，复制并执行 `init.sh`：

```bash
DEVICE_HOSTNAME="m02"

ssh-keygen -R 192.168.55.1
scp init.sh ubuntu@192.168.55.1:/tmp/
ssh -t ubuntu@192.168.55.1 \
  "sudo bash /tmp/init.sh '$DEVICE_HOSTNAME'"
```

`init.sh` 会执行：

1. 写入 `/etc/hostname`，替换 `/etc/hosts` 的 `127.0.1.1`。
2. 重建 `/etc/machine-id` 和 `/var/lib/dbus/machine-id`。
3. 删除克隆来的 SSH host key，生成 RSA、ECDSA 和 ED25519 新 key。
4. 清理 random-seed、USB Device Mode MAC、NetworkManager 状态、蓝牙状态和日志。
5. 自动重启。

因为 SSH host key 已变化，管理主机重新连接前需要删除旧记录：

```bash
ssh-keygen -R 192.168.55.1
ssh ubuntu@192.168.55.1
```

### 阶段 ⑪：验证目标机

验证：

```bash
hostnamectl --static                              # 各机不同
cat /etc/machine-id                              # 各机不同
ssh-keygen -lf /etc/ssh/ssh_host_ed25519_key.pub # 各机指纹不同
cat /opt/nvidia/l4t-usb-device-mode/mac-addresses # 各机 MAC 不同
```

---

## 六、仓库脚本

### clean.sh

`clean.sh` 只在黄金母机最终净化时运行。它具有以下保护：

- 必须以 root 执行。
- 必须是 Jetson Linux。
- 默认要求输入 `CLEAN`。
- 发现固定网络绑定时拒绝执行。
- 保留 SSH host key、hostname、NetworkManager 连接配置、`authorized_keys` 和用户 SSH 私钥。
- 净化完成后自动关机。

```bash
sudo ./clean.sh --check

sudo ./clean.sh

# 自动化模式
sudo ./clean.sh --yes

# 同时删除 apt lists
sudo ./clean.sh --purge-apt-lists
```

> 不要在已经投入使用的正常设备上执行 `clean.sh`。

### init.sh

`init.sh` 只在目标机刷完第一次开机后执行。它接收目标 hostname，并完成最终差异化：

```bash
sudo ./init.sh m02

# 或显式参数
sudo ./init.sh --hostname m02
```

默认会自动重启。调试时可使用：

```bash
sudo ./init.sh m02 --no-reboot
```

> 重复执行 `init.sh` 会再次更换 machine-id 和 SSH host key。正常生产流程只执行一次。

---

## 七、命令速查表

| 阶段              | 命令核心                                                      | 在哪执行                |
| ----------------- | ------------------------------------------------------------- | ----------------------- |
| 0 主机准备        | `apply_binaries.sh`                                           | x86 主机                |
| ① 生成最小 rootfs | `nv_build_samplefs.sh --flavor minimal`                       | x86 主机                |
| ② 装入 rootfs     | `tar xpf sample_fs.tbz2 -C rootfs/` + `apply_binaries.sh`     | x86 主机                |
| ③ 锁无头          | `ln -sf multi-user.target .../default.target`                 | x86 主机                |
| ④ 预置用户        | `l4t_create_default_user.sh -u .. -p .. -a --accept-license`  | x86 主机                |
| ⑤ 刷母机          | `l4t_initrd_flash.sh ... external`                            | 主机（母机 recovery）   |
| ⑥ 装组件          | `apt install nvidia-jetpack-runtime`                          | 母机                    |
| ⑦ 母机净化        | 运行 `clean.sh`，清状态并自动关机                             | 母机内部                |
| ⑧ 制作黄金镜像    | `l4t_backup_restore.sh -b -c -e nvme0n1 ...`                  | 主机（母机 recovery）   |
| ⑨ 刷目标机        | `l4t_backup_restore.sh -r -e nvme0n1 ...` 或 massflash        | 主机（目标机 recovery） |
| ⑩ 首启初始化      | `init.sh m02`                                                 | 目标机                  |
| ⑪ 首启验证        | 检查 hostname、machine-id、SSH key 和 USB MAC                 | 目标机                  |

---

## 八、常见坑与注意事项

- **版本一致**：主机 BSP 与母机 L4T 的大版本 + 小版本必须完全相同，否则易变砖。
- **eMMC vs NVMe**：本指南针对 **NVMe**。eMMC 用 `internal` target 且可用老的 `flash.sh -r -k APP`；NVMe 必须用 `external` + initrd flash / backup_restore。母机与目标机存储介质必须一致。
- **纯净的正确姿势**：从 `minimal` rootfs 往上装，不要刷 desktop 再删 GUI。
- **无头防卡死**：务必预置用户 + 锁 multi-user，避免开机弹 oem-config 却无屏幕操作。
- **身份生成时机**：目标机首次启动时 systemd 可能先生成 machine-id；`init.sh` 会再次明确重建 machine-id，同时设置 hostname 并重建 SSH host key，随后重启使所有服务使用最终身份。
- **临时共享 SSH 身份**：目标机第一次执行 `init.sh` 前会临时共用母机 SSH host key，只能在可信部署网络里短暂使用。
- **净化顺序**：完成母机配置 → 运行 `clean.sh` → 自动关机 → 立即读取镜像，中途不要正常开机。
- **init.sh 后要重连**：`init.sh` 会重建 SSH host key 并重启，管理主机需执行 `ssh-keygen -R 192.168.55.1` 后再连。
- **USB Hub**：批量刷机务必用**带独立供电**的 USB Hub，无源 Hub 会因瞬时功耗掉盘。
- **`-h` 自查**：`nv_build_samplefs.sh` 的 flavor 名、`l4t_backup_restore.sh` 的参数在不同 36.x 小版本略有差异，动手前先 `-h` 确认。
- **CUDA 不破坏无头**：`nvidia-jetpack` 不会装回桌面，可放心安装。

---

## 官方文档出处

- NVIDIA Jetson Linux Developer Guide → **"Headless Mode" / "Configuring a Headless System"**
- **Flashing Support → l4t_create_default_user**
- **samplefs → nv_build_samplefs**
- **Image-Based OTA / backup_restore / initrd flash（massflash）**
- 下载页：https://developer.nvidia.com/embedded/jetson-linux

---

_本文档由 Jetson AGX Orin 无头系统 + 批量部署系列对话整理生成。执行前请以你实际的 L4T 36.x 小版本官方文档为准。_
