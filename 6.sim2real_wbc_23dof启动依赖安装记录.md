# sim2real_wbc_23dof 启动依赖安装记录

记录日期：2026-07-15

目标主机：

- SSH：`ubuntu@192.168.10.142`
- 主机名：`m01`
- 系统：Ubuntu 22.04.5 LTS
- 架构：ARM64 / NVIDIA Jetson AGX Orin
- Jetson Linux：R36.5
- 内核：`5.15.185-tegra`
- Python：3.10.12
- ROS2：Humble

目标程序的实际文件名使用下划线，不是连字符：

```text
/home/ubuntu/Code/rl_control_ws/src/full_body_rl_control/
  learning_control/src/sim2sim/sim2real_wbc_23dof.py
```

## 1. 最终结果

已经完成：

- 补齐 `pygame` 和 ZeroMQ Python 依赖。
- 安装 CUDA 12.6、cuDNN 9.3、CUPTI、NVTX 和 cuSPARSELt。
- 安装 TensorRT 10.3 及其 Python 绑定、ONNX 解析器和 DLA 依赖。
- 安装 NVIDIA Jetson CUDA 版 PyTorch 2.5。
- 将 NumPy 升级到项目要求的 1.24.4。
- 修正配置文件中错误的 `/home/m03/...` 绝对路径。
- 增加 TensorRT 10 API 兼容代码。
- 在当前 Orin 上重新生成两套 TensorRT engine。
- 构建 `motor_master_control`、`yesense_interface`、
  `yesense_std_ros2` 和 `learning_control`。
- 实测 Yesense IMU 能发布 `/imu_data_ros`。
- 将用户 `ubuntu` 加入 `dialout` 和 `input` 组。

软件侧验证结果：

```text
PyTorch:       2.5.0a0+872d972e41.nv24.08
PyTorch CUDA:  12.6
CUDA available: True
CUDA device:   Orin
TensorRT:      10.3.0
disturb policy backend: tensorrt
running policy backend: tensorrt
```

当前不能完整进入实机控制循环的原因不是缺软件，而是两项物理设备未连接：

1. 没有检测到 Logitech/兼容手柄，`/dev/input/js*` 不存在。
2. 没有检测到 KH-UCANFD USB 设备，因此 `can0` 到 `can4` 不存在。

Yesense IMU 当前已连接，设备为 `/dev/ttyACM0`。

## 2. 脚本实际需要的依赖

| 依赖 | 作用 | 当前状态 |
| --- | --- | --- |
| ROS2 Humble `rclpy` | 创建 ROS2 节点，订阅 IMU 和电机反馈，发布电机控制数据 | 原来已有 |
| `sensor_msgs` | 使用 `Imu` 和 `JointState` 消息 | 原来已有 |
| `std_msgs` | 使用 `Float64MultiArray` 发布电机控制数组 | 原来已有 |
| NumPy | 观测、动作、关节数据和矩阵运算 | 已升级到 1.24.4 |
| PyYAML | 读取 `config_wbc_23dof.yaml` | 原来已有 |
| SciPy | 四元数和旋转变换 | 原来已有 |
| Pygame | 读取 Logitech 手柄摇杆和按键 | 本次安装 |
| pyzmq | 向 PlotJuggler 发布控制数据 | 本次安装 |
| PyTorch | 加载 `.pt` 策略，并为 TensorRT 提供 CUDA Tensor 显存 | 本次安装 CUDA 版 |
| CUDA | Orin GPU 运行时和基础计算库 | 本次安装 |
| cuDNN | PyTorch 神经网络 GPU 算子 | 本次安装 |
| TensorRT | 将 ONNX 策略编译为 engine 并执行低延迟推理 | 本次安装 |
| cuSPARSELt | NVIDIA PyTorch wheel 导入时需要的稀疏矩阵库 | 本次手动安装 |
| Yesense ROS2 驱动 | 从 `/dev/ttyACM0` 读取 IMU 并发布 `/imu_data_ros` | 原来有源码，本次重新构建并实测 |
| KH-UCANFD `kcan` | 将外接 USB-CANFD 设备注册为 SocketCAN 接口 | 原来已经安装并加载 |
| `motor_master_control` | 通过 `can0` 到 `can4` 控制 32 个电机并发布反馈 | 原来有源码，本次重新构建 |
| Linux `joydev` | 为 USB 手柄提供 joystick 输入接口 | 内核已有，本次加载 |
| Linux `cdc_acm` | 将 Yesense USB CDC 设备注册为 `/dev/ttyACM0` | 内核已有且已加载 |

## 3. 逐项安装记录

### 3.1 更新 APT 索引

执行：

```bash
sudo apt-get update
```

作用：

- 刷新 Ubuntu、NVIDIA Jetson R36.5 和 ROS2 软件源索引。
- 确保后续安装使用当前仓库中的匹配版本。

结果：成功。

### 3.2 安装 Pygame

执行：

```bash
sudo apt-get install -y python3-pygame
```

作用：

- 脚本通过 `pygame.joystick` 读取手柄。
- 读取摇杆速度命令、A/B/X/Y/LB/RB 等按键。

安装版本：

```text
python3-pygame 2.1.2+dfsg-3
```

验证：

```bash
python3 -c "import pygame; print(pygame.version.ver)"
```

结果：`2.1.2`，导入成功。

### 3.3 安装 pyzmq

执行：

```bash
sudo apt-get install -y python3-zmq
```

作用：

- 提供 Python ZeroMQ 模块 `zmq`。
- 脚本在 TCP 5555 端口发布控制状态和关节数据，供 PlotJuggler 使用。

安装版本：

```text
python3-zmq 22.3.0-1build1
```

验证：

```bash
python3 -c "import zmq; print(zmq.__version__)"
```

结果：`22.3.0`，导入成功。

### 3.4 安装 NVIDIA CUDA 运行库

执行：

```bash
sudo apt-get install -y nvidia-cuda
```

作用：

- 安装 JetPack 6.2.2 对应的 CUDA 12.6 运行环境。
- 提供 cuBLAS、cuFFT、cuSOLVER、cuSPARSE、NPP、NVRTC 等 GPU 库。
- PyTorch 和 TensorRT 都依赖这些动态库。

安装版本：

```text
nvidia-cuda 6.2.2+b24
CUDA runtime 12.6
```

本次下载约 766 MB，安装后约占 1.8 GB。

### 3.5 安装 cuDNN

执行：

```bash
sudo apt-get install -y nvidia-cudnn
```

作用：

- 为 PyTorch 提供卷积、归一化等神经网络 GPU 算子。
- 与当前 JetPack CUDA 12 环境匹配。

安装版本：

```text
nvidia-cudnn 6.2.2+b24
libcudnn9-cuda-12 9.3.0.75-1
```

### 3.6 安装 CUPTI

执行：

```bash
sudo apt-get install -y cuda-cupti-12-6
```

作用：

- CUDA Profiling Tools Interface。
- NVIDIA PyTorch 运行库可能链接该组件，也可用于 CUDA 性能分析。

安装版本：

```text
cuda-cupti-12-6 12.6.68-1
```

### 3.7 安装 TensorRT

执行：

```bash
sudo apt-get install -y tensorrt
```

作用：

- 提供 TensorRT C++ 和 Python 运行时。
- 提供 ONNX 解析器，将 `.onnx` 策略构建为 `.engine`。
- 脚本优先使用 TensorRT，以降低 50 Hz 控制循环中的推理延迟。

安装版本：

```text
tensorrt 10.3.0.30-1+cuda12.5
Python tensorrt 10.3.0
```

说明：

- 该版本来自 NVIDIA Jetson R36.5 官方仓库。
- 包名中的 `cuda12.5` 是 NVIDIA 对 TensorRT 构建包的版本标记。
- 已在本机 CUDA 12.6 环境完成 engine 构建和推理 warmup。

### 3.8 安装 Jetson DLA 编译器库

TensorRT 首次导入时提示：

```text
ImportError: libnvdla_compiler.so: cannot open shared object file
```

执行：

```bash
sudo apt-get install -y nvidia-l4t-dla-compiler
sudo ldconfig
```

作用：

- 提供 Jetson DLA 编译器动态库 `libnvdla_compiler.so`。
- `ldconfig` 将新动态库写入系统链接器缓存。

安装版本：

```text
nvidia-l4t-dla-compiler 36.5.0-20260115194252
```

验证：

```bash
python3 -c "import tensorrt as trt; print(trt.__version__)"
```

结果：`10.3.0`。

### 3.9 安装 NVIDIA Jetson CUDA 版 PyTorch

执行：

```bash
python3 -m pip install --user --no-cache-dir \
  "https://developer.download.nvidia.cn/compute/redist/jp/v61/pytorch/torch-2.5.0a0+872d972e41.nv24.08.17622132-cp310-cp310-linux_aarch64.whl"
```

作用：

- 加载项目中的 TorchScript `.pt` 策略。
- 当 TensorRT 不可用时作为 GPU/CPU 回退后端。
- 当前 TensorRT 封装也使用 PyTorch CUDA Tensor 管理显存。

安装版本：

```text
torch 2.5.0a0+872d972e41.nv24.08
torch CUDA 12.6
```

### 3.10 安装 CUDA NVTX

PyTorch 首次导入时提示缺少：

```text
libnvToolsExt.so.1
```

执行：

```bash
sudo apt-get install -y cuda-nvtx-12-6
sudo ldconfig
```

作用：

- 提供 `libnvToolsExt.so.1`。
- NVTX 用于 CUDA 时间线标记和性能分析，当前 NVIDIA PyTorch wheel 会链接它。

安装版本：

```text
cuda-nvtx-12-6 12.6.68-1
```

### 3.11 安装 cuSPARSELt

PyTorch 继续导入时提示缺少：

```text
libcusparseLt.so.0
```

执行：

```bash
mkdir -p /tmp/codex-cusparselt-0.6.2.3
cd /tmp/codex-cusparselt-0.6.2.3

curl -fL --retry 3 -O \
  "https://developer.download.nvidia.cn/compute/cusparselt/redist/libcusparse_lt/linux-sbsa/libcusparse_lt-linux-sbsa-0.6.2.3-archive.tar.xz"

tar -xf libcusparse_lt-linux-sbsa-0.6.2.3-archive.tar.xz

sudo cp -a \
  libcusparse_lt-linux-sbsa-0.6.2.3-archive/include/. \
  /usr/local/cuda/include/

sudo cp -a \
  libcusparse_lt-linux-sbsa-0.6.2.3-archive/lib/. \
  /usr/local/cuda/targets/aarch64-linux/lib/

sudo ldconfig
```

作用：

- 为 PyTorch 提供 GPU 稀疏矩阵乘法库。
- 解决 PyTorch 导入阶段对 `libcusparseLt.so.0` 的链接需求。

安装版本：

```text
cuSPARSELt 0.6.2.3
```

验证路径：

```text
/usr/local/cuda-12.6/targets/aarch64-linux/lib/libcusparseLt.so.0.6.2.3
```

### 3.12 将 NumPy 升级到项目指定版本

PyTorch 导入时发现原系统 NumPy 1.21.5 的 C API 过旧。项目
`learning_control/src/sim2sim/setup.py` 也明确要求 `numpy==1.24.4`。

执行：

```bash
python3 -m pip install --user --no-cache-dir "numpy==1.24.4"
```

作用：

- 满足 PyTorch wheel 的 NumPy C API 要求。
- 保持与项目 `setup.py` 中固定版本一致。

结果：

```text
numpy 1.24.4
```

### 3.13 配置串口和手柄权限

执行：

```bash
sudo usermod -aG dialout,input ubuntu
```

作用：

- `dialout`：允许用户访问 `/dev/ttyACM0`，不再依赖 `chmod 777`。
- `input`：允许用户读取 `/dev/input/event*` 和手柄输入设备。

重新 SSH 登录后验证：

```text
ubuntu groups include: dialout, input
/dev/ttyACM0 read/write: OK
```

### 3.14 加载手柄内核模块

执行：

```bash
sudo modprobe joydev
```

作用：

- 加载 Linux joystick 输入接口。
- Logitech 手柄插入后可生成 `/dev/input/js0` 等设备。

结果：`joydev` 已加载。

当前没有插入手柄，所以仍然没有 `/dev/input/js*`。

## 4. 原来已经存在，不需要重复安装的驱动

### 4.1 ROS2 Humble

已安装：

```text
ros-humble-ros-base 0.10.0-1jammy.20260607.150006
ros-humble-rclpy
ros-humble-sensor-msgs
ros-humble-std-msgs
ros-humble-geometry-msgs
```

作用：

- 提供目标脚本使用的 ROS2 Python API 和消息类型。

### 4.2 KH-UCANFD `kcan`

当前模块信息：

```text
driver:  kcan
version: Release_20260514_n
path:    /lib/modules/5.15.185-tegra/misc/kcan.ko
```

当前已加载模块：

```text
kcan
can_dev
can
can_raw
can_bcm
```

作用：

- `kcan`：昆宏 KH-UCANFD 厂商 USB-CANFD 驱动。
- `can_dev`：Linux CAN 网络设备公共层。
- `can_raw`：SocketCAN RAW socket，电机程序用它收发 CAN 帧。
- `can_bcm`：SocketCAN Broadcast Manager。

当前问题：

- `lsusb` 没有看到 KH-UCANFD 设备。
- `ip link show type can` 没有任何 CAN 接口。
- 电机配置要求至少出现 `can0`、`can1`、`can2`、`can3`、`can4`。

因此驱动软件已在，但必须连接实际 KH-UCANFD 硬件后才能继续验证。

### 4.3 Yesense USB 串口驱动

当前 USB 设备：

```text
5953:5543 Yesense Co.Ltd YESENSE USB CDC ACM
```

当前内核模块：

```text
cdc_acm
```

作用：

- 将 Yesense IMU 注册为 `/dev/ttyACM0`。

无需额外安装厂商内核驱动。

## 5. 项目配置和代码修正

### 5.1 修正模型和动作文件路径

修改文件：

```text
learning_control/src/sim2sim/config_wbc_23dof.yaml
```

原配置写死为不存在的：

```text
/home/m03/Code/...
```

已改成仓库内相对路径：

```yaml
policy_path: "pt/policy_20260422_182136.pt"
onnx_policy_path: "onnx/cy/policy_20260422_182136.onnx"
policy_path: "pt/policy.pt"
onnx_policy_path: "onnx/cy/policy.onnx"
upper_motion_dir: "UpperJointMotionData"
```

作用：

- 适配当前 `/home/ubuntu` 用户。
- 将来移动仓库时不需要再次修改用户名绝对路径。

### 5.2 增加 TensorRT 10 API 兼容

修改文件：

```text
learning_control/src/sim2sim/sim2real_wbc_23dof.py
```

原代码使用 TensorRT 8 风格 API：

```text
engine.num_bindings
engine.get_binding_shape()
context.execute_v2()
```

TensorRT 10.3 已改为 tensor-name API。兼容代码现在支持：

```text
engine.num_io_tensors
engine.get_tensor_name()
engine.get_tensor_shape()
context.set_tensor_address()
context.execute_async_v3()
```

同时保留旧版 binding API 回退。

结果：两套策略均实际使用 `tensorrt`，不再回退到 PyTorch。

### 5.3 在当前 Orin 上重建 TensorRT engine

生成文件：

```text
learning_control/src/sim2sim/onnx/cy/policy.engine
learning_control/src/sim2sim/onnx/cy/policy_20260422_182136.engine
```

原仓库中的 `policy.engine` 来自其他设备，TensorRT 曾输出跨设备警告。
原文件已备份到：

```text
/home/ubuntu/Code/rl_control_ws/engine_backup/policy.engine.pre-m01
```

当前两个 engine 都已在 `m01` 的 Orin 上构建并完成 5 次 warmup。

## 6. ROS2 构建结果

执行：

```bash
cd ~/Code/q2_robot_ws
source /opt/ros/humble/setup.bash
colcon build --packages-select motor_master_control
```

结果：

```text
Summary: 1 package finished
motor_master_control/can_test 已生成
```

执行：

```bash
cd ~/Code/rl_control_ws
source /opt/ros/humble/setup.bash
colcon build --packages-select \
  yesense_interface yesense_std_ros2 learning_control
```

结果：

```text
Summary: 3 packages finished
```

可执行程序：

```text
yesense_std_ros2 yesense_node_publisher
yesense_std_ros2 yesense_node_subscriber
motor_master_control can_test
```

## 7. 验证记录

### 7.1 PyTorch CUDA 实际运算

验证命令执行了一个 CUDA Tensor 平方运算。

结果：

```text
torch CUDA available: True
device count: 1
device name: Orin
input:  [1.0, 2.0]
result: [1.0, 4.0]
```

### 7.2 TensorRT 策略加载

结果：

```text
disturb: TensorRT engine 加载成功，warmup 5 次
running: TensorRT engine 加载成功，warmup 5 次
backend: [('disturb', 'tensorrt'), ('running', 'tensorrt')]
```

对相同随机观测比较 TensorRT FP16 与 PyTorch CUDA FP32：

```text
disturb action max abs error:  0.006094
disturb velocity max abs error: 0.003198
running action max abs error:  0.020364
running velocity max abs error: 0.001261
```

结果符合 FP16 推理相对 FP32 的预期误差量级，输出数量和顺序一致。

### 7.3 Yesense IMU 实测

启动 Yesense 节点后执行：

```bash
ros2 topic echo /imu_data_ros --once
```

成功收到：

- 四元数姿态。
- 三轴角速度。
- 三轴线加速度。
- 静止时 Z 轴加速度约 `9.796 m/s^2`。

说明 Yesense USB、串口权限、ROS2 驱动和消息发布链路正常。

### 7.4 目标脚本启动预检

目标脚本已经完成：

- 配置读取。
- 两套策略 TensorRT 加载。
- 两套策略 warmup。
- 观测处理器初始化。

随后停止于：

```text
AssertionError: 未检测到手柄！
```

该检查发生在 ROS 电机接口初始化之前，因此这次预检没有发布电机控制命令。

## 8. 当前硬件阻塞项

### 8.1 手柄未连接

当前：

```text
/dev/input/js* 不存在
pygame.joystick.get_count() == 0
```

处理：

1. 插入 Logitech 或 SDL 兼容 USB 手柄。
2. 重新登录 SSH，确保 `ubuntu` 用户包含 `input` 组。
3. 检查：

```bash
ls -l /dev/input/js*
python3 -c "import pygame; pygame.init(); pygame.joystick.init(); print(pygame.joystick.get_count())"
```

数量必须大于 0。

### 8.2 KH-UCANFD 未连接

当前：

```text
没有 KH-UCANFD USB ID
没有 can0 到 can4
```

处理：

1. 插入 KH-UCANFD 硬件。
2. 检查：

```bash
lsusb
lsmod | grep kcan
ip -brief link show type can
```

电机程序配置需要：

```text
can0 can1 can2 can3 can4
```

没有这些接口时不要启动电机主节点。

## 9. 实机启动顺序

启动前必须满足：

- 机器人有可靠支撑或吊架。
- 急停可用。
- 操作区域无人。
- Yesense 已连接。
- Logitech 手柄已连接。
- KH-UCANFD 已连接，并出现 `can0` 到 `can4`。

### 终端 1：启动 IMU

```bash
cd ~/Code/rl_control_ws
source /opt/ros/humble/setup.bash
source install/setup.bash

export LD_LIBRARY_PATH="$PWD/install/yesense_std_ros2/lib/yesense_std_ros2/serial:$LD_LIBRARY_PATH"

ros2 run yesense_std_ros2 yesense_node_publisher \
  --ros-args \
  --params-file "$PWD/src/full_body_rl_control/sensors_driver/yesense/yesense_std_ros2/config/yesense_config.yaml"
```

检查：

```bash
ros2 topic echo /imu_data_ros --once
```

### 终端 2：启动电机主节点

确认 `can0` 到 `can4` 存在后：

```bash
cd ~/Code/q2_robot_ws
source /opt/ros/humble/setup.bash
source install/setup.bash

sudo -v
ros2 run motor_master_control can_test
```

说明：

- 电机程序内部使用 `sudo ip link` 配置 CAN FD。
- `sudo -v` 先缓存管理员凭据，避免程序内部反复等待密码。
- 该节点会接触真实电机，必须在完成实机安全检查后启动。

检查反馈话题：

```bash
ros2 topic echo /motor/q2w_upper_motor_joint_state --once
```

消息中的 `position` 长度必须为 32。

### 终端 3：启动 23 DOF WBC

确认 IMU 和电机反馈都正常后：

```bash
cd ~/Code/rl_control_ws/src/full_body_rl_control/learning_control/src/sim2sim
source /opt/ros/humble/setup.bash
source ~/Code/rl_control_ws/install/setup.bash

python3 sim2real_wbc_23dof.py
```

脚本需要同时收到：

```text
/imu_data_ros
/motor/q2w_upper_motor_joint_state
```

脚本会发布：

```text
/motor/q2w_upper_motor_pt_control_data
```

## 10. 当前代码仓库状态

远程仓库当前有以下未提交修改：

```text
M  learning_control/src/sim2sim/config_wbc_23dof.yaml
M  learning_control/src/sim2sim/sim2real_wbc_23dof.py
M  learning_control/src/sim2sim/onnx/cy/policy.engine
?? learning_control/src/sim2sim/onnx/cy/policy_20260422_182136.engine
```

这些修改分别是：

- 路径改为相对路径。
- TensorRT 10 API 兼容。
- 在当前 Orin 上重建 running engine。
- 在当前 Orin 上新建 disturb engine。

当前根分区剩余空间约 874 GB。
